extends layout
block content
    .wrapper.wrapper-content.animated.fadeInRight
        
       .loginscreen.text-center.animated.fadeInDown
            h3 Edit member to IC+
            p Edit member to see it in action.
                form.m-t(role='form', action='', method='post',enctype='multipart/form-data')
                    .form-group
                        input.form-control(type='text', name='name', placeholder='Name')
                    .form-group
                        input.form-control(type='text', placeholder='Author',name='author')
                    .form-group
                        input.form-control(type='text',placeholder='Description',name='description')
                    .form-group
                        select.form-control(name='parent')
                            option(value="0") Danh mục cha
                            if categories
                                each category in categories
                                    
                                    option(value="#{category.id}") #{category.name}
                    
                    input.btn.btn-primary.block.full-width.m-b(type='submit', name='submit', value='Edit')